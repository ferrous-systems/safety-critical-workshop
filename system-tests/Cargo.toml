[package]
name = "system-tests"
version = "0.1.0"
edition = "2024"

[dependencies]
mantra-macros = { workspace = true, features = ["log"] }
embsinth = "0.2.0"
log = "0.4.33"

[features]
phase-one = []
phase-two = ["phase-one"]
