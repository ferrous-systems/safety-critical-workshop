[package]
name = "rad"
version = "0.1.0"
edition = "2021"

[dependencies]
nrf-hal = { path = "../nrf-hal", optional = true }
defmt = { version = "1", optional = true }
mantra-macros = "0.8.0"

[dev-dependencies]
mockall = "0.15.0"
mockall_double = "0.3.1"

[features]
hw = ["nrf-hal", "defmt"]
